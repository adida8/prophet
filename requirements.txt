httpx>=0.27,<1.0
websockets>=13.0,<14.0
cryptography>=43.0,<45.0
pandas>=2.2,<3.0
python-dotenv>=1.0,<2.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
aiosqlite>=0.20,<1.0
# Postgres driver — used by `activity/` (Railway Postgres add-on, the
# activity-signals data layer). Other domains still use sqlite.
asyncpg>=0.29,<1.0
# Used by desk/desk/signals/extract.py (Haiku-driven Signal extraction).
# The desk package isn't pip-installed on Railway — it runs as a
# subprocess module — so its own pyproject deps don't apply here.
anthropic>=0.40,<1.0
# Daily report template + PDF rendering (daily_report.py, daily_report_loop.py).
# WeasyPrint depends on Cairo + Pango at the system level; on the Railway
# image both are present. daily_report.py falls back to HTML-only when
# WeasyPrint import or render fails so the email still goes out.
jinja2>=3.1,<4.0
weasyprint>=62.0,<70.0
