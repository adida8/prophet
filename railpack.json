{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "node": "20"
  },
  "steps": {
    "frontend": {
      "inputs": [
        { "step": "packages:mise" },
        { "local": true, "include": ["frontend"] }
      ],
      "commands": [
        "cd frontend && npm ci",
        "cd frontend && npm run build"
      ],
      "caches": ["npm"]
    }
  },
  "caches": {
    "npm": {
      "directory": "/root/.npm",
      "type": "shared"
    }
  },
  "deploy": {
    "startCommand": "python main.py --dashboard --port $PORT",
    "inputs": [
      { "step": "build" },
      { "step": "frontend", "include": ["frontend/dist"] }
    ]
  }
}
